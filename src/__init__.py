# src package
# This package will contain the following modules:
#   data_cleaning.py   — load raw CSV, fix invalid values, impute missing data
#   features.py        — feature engineering (distance, hour-of-day, encodings)
#   train.py           — model training and cross-validation
#   predict.py         — load saved model and return predictions
